# Игровая Платформа - Инструкция по запуску

Этот проект представляет собой игровую платформу на Next.js с двумя типами игр:
- **Найди Слово** (Word Search Game)
- **Quiz викторина** (Kahoot-style)

## Требования

- **Node.js** версии 18 или выше
- **pnpm** (менеджер пакетов)
- **Supabase** аккаунт (бесплатный)

## Шаг 1: Установка зависимостей

Если у вас не установлен pnpm, установите его:
```bash
npm install -g pnpm
```

Затем установите зависимости проекта:
```bash
pnpm install
```

## Шаг 2: Настройка Supabase

1. Создайте аккаунт на [supabase.com](https://supabase.com) (если еще нет)
2. Создайте новый проект
3. Перейдите в **Settings** → **API**
4. Скопируйте следующие значения:
   - **Project URL** (это будет `NEXT_PUBLIC_SUPABASE_URL`)
   - **anon public** key (это будет `NEXT_PUBLIC_SUPABASE_ANON_KEY`)

## Шаг 3: Создание файла с переменными окружения

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_project_url_из_supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_anon_key_из_supabase
```

**Важно:** Замените значения на реальные из вашего Supabase проекта!

## Шаг 4: Настройка базы данных

1. В Supabase перейдите в **SQL Editor**
2. Выполните скрипт `scripts/001_create_game_tables.sql` (создаст таблицы для игр)
3. Затем выполните скрипт `scripts/002_create_quiz_and_admin_tables.sql` (создаст таблицы для викторин и админ-панели)

Эти скрипты создадут все необходимые таблицы и политики безопасности.

**Примечание:** В скрипте `002_create_quiz_and_admin_tables.sql` уже есть демо-админ:
- **Username:** `admin`
- **Password:** `admin123`

## Шаг 5: Запуск проекта

### Режим разработки:
```bash
pnpm dev
```

Проект будет доступен по адресу: **http://localhost:3000**

### Сборка для продакшена:
```bash
pnpm build
pnpm start
```

## Проверка работоспособности

После запуска откройте браузер и перейдите на `http://localhost:3000`. Вы должны увидеть главную страницу с двумя карточками игр.

### Что проверить:
1. ✅ Главная страница загружается
2. ✅ Можно перейти в "Найди Слово"
3. ✅ Можно перейти в админ-панель (`/admin`)
4. ✅ В админ-панели можно войти (admin/admin123)

## Возможные проблемы

### Ошибка подключения к Supabase
- Проверьте, что переменные окружения в `.env.local` указаны правильно
- Убедитесь, что проект в Supabase активен
- Проверьте, что таблицы созданы в базе данных

### Ошибки при установке зависимостей
- Убедитесь, что используете Node.js версии 18+
- Попробуйте удалить `node_modules` и `pnpm-lock.yaml`, затем запустите `pnpm install` снова

### Порт 3000 занят
- Измените порт: `pnpm dev -- -p 3001`

## Структура проекта

- `/app` - страницы Next.js (App Router)
- `/components` - React компоненты
- `/lib/supabase` - клиенты Supabase
- `/scripts` - SQL скрипты для базы данных
- `/public` - статические файлы

## Дополнительная информация

- Проект использует **Next.js 16** с App Router
- Стилизация через **Tailwind CSS**
- UI компоненты из **Radix UI**
- База данных: **Supabase (PostgreSQL)**

---

**Удачной игры! 🎮**
